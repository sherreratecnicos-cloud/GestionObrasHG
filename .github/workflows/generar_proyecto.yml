name: Generar Proyecto iOS 26

on:
  workflow_dispatch:

jobs:
  build-and-generate:
    runs-on: macos-latest

    steps:
      - name: Checkout del repositorio
        uses: actions/checkout@v4

      - name: Mostrar versiones de Xcode y Swift
        run: |
          xcodebuild -version
          swift --version

      - name: Ejecutar script de generación
        run: |
          chmod +x ./generar_proyecto.sh
          ./generar_proyecto.sh

      - name: Commit y push de archivos generados
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add GestionObrasHG
          git commit -m "📦 Archivos del proyecto generados automáticamente" || echo "No hay cambios"
          git push
